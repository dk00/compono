{
  "name": "ucompass",
  "version": "0.0.1",
  "description": "Minimalist HTML5 compass",
  "scripts": {
    "start": "browser-sync -w",
    "build": "mkdir -p public && node build.js && cp manifest.json favicon.png public/",
    "publish": "npm run build && netlify deploy"
  },
  "license": "Unlicense",
  "private": true,
  "installConfig": {
    "pnp": true
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "inline-source": "^6.1.8"
  }
}
